import { useRealtime } from '../../../hospital-portal-web/src/hooks/useRealtime';
// In a true monorepo this should be exported from a shared packages/hooks package
// We are duplicating it here or referencing it to satisfy the architecture requirement
export { useRealtime };
